# Data source: local (default, localStorage) | supabase
# Switch to supabase when the app is wired to getRepository() end-to-end.
VITE_DATA_SOURCE=local

# Supabase project: ggapbctlajwmbjvbfrsq (ap-northeast-2)
# Copy real values into .env (gitignored). Anon key from Dashboard → Settings → API.
VITE_SUPABASE_URL=https://ggapbctlajwmbjvbfrsq.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
